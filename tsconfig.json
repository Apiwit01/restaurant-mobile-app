{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    // --- เพิ่มบรรทัดนี้เข้าไป ---
    "baseUrl": ".",
    "paths": {
      "@/*": [
        "./*"
      ]
    }
  },
  "include": [
    "**/*.ts",
    "**/*.tsx",
    ".expo/types/**/*.ts",
    "expo-env.d.ts"
  ]
}